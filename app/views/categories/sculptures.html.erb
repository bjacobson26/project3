<%= render 'partials/break' %>


<div class="container">
  <div class="row">
  	<% @sculptures.each do |sculpture| %>
	  <div class="col-md-4"><%= link_to sculpture.name, product_path(sculpture.id), :class => "title" %><%= image_tag sculpture.product_images.first.image, :class=>'catimages' %><%= link_to "More Info", product_path(sculpture.id), class: 'btn btn-primary', style: "display:block;"%>
	  </div>
	  <% end %>
	</div>
	
</div>

<%= render 'partials/break' %>
<%= render 'partials/break' %>
<%= render 'partials/break' %>
